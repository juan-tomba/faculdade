Pergunta:
"Em Haskell, o ""$"" é uma função que serve para determinar precedências, enquanto o ""."" é uma função que acessa os atributos de um struct"
"Em Java, os trechos funcionais são executados de forma lazy, já em Ruby, não."
"Dada a função ""f :: Int -> Int"", definida como ""f y = y * 2"". Se for aplicada ""f 5"" o resultado será 10."
"Haskell é uma linguagem multiparadigma, a qual permite o desenvolvimento de software de forma imperativa, orientada a objetos e funcional."
A programação funcional não está diretamente relacionada ao cálculo lambda
"Funções anônimas são funções sem nome, geralmente usadas como parâmetros de funções."
"Em Haskell existem as instruções ""if"", ""then"" e ""else""."
É aceitável declarar uma lista infinita em Haskell desde que o programa não precise utilizá-la por completo?
"O tipo Integer pode armazenar o fatorial de 13, enquanto o tipo Int não "
"Ao criar um tipo de dados customizado, para usar o método ""show"" na visualização de informações das instâncias do novo tipo, é necessário também definir um mapeamento de representação da instância em string. A única forma possível de realizar tal mapeamento é utilizando deriving."
C é uma linguagem funcional.
"É possível passar uma função como argumento em C, utilizando ponteiros de função."
"Em Java, mesmo em um trecho de código funcional, não será executado de forma preguiçosa (lazy)."
"Uma lista declarada como “[3,5..]” começa no 3 e vai de 5 em 5 até o infinito"
"A linguagem Haskell possui avaliação lazy, ou seja, as expressões são avaliadas somente quando seu valor é realmente necessário."
Haskell possui suporte nativo para funções lambda?
Tupla é um tipo de estrutura de dados do Python que tem como principal característica ser imutável
"A função f = [n * (n + 1) `div` 2 | n <- [1..]] retorna a lista [1, 3, 6, 10, 15 ...]"
"Ao invés de usar recursão, como é comum em paradigmas imperativos, a programação funcional é baseada na utilização de laços para a construção de programas"
"Na programação funcional não é possível realizar atividades repititivas, porque não se usa a estrutura 'for'"
filtro :: (a -> Bool) -> [a] -> [a].essa função retorna uma lista que contém apenas os elementos da lista de entrada para os quais a função de teste retorna true
Ruby off rails foi o framework que fez Ruby se tornar popular?
"A função . (ponto), utilizada para composição de funções, tem tipo (a->b) -> (a->b) -> (a->b)"
Ruby on Rails não deu certo por conta da demora e dificuldade para construir projetos. 
O where serve para localizar uma função dentro de outra.
Todo e qualquer problema é melhor resolvido por programação funcional.
Programação funcional utiliza de recursão?
A diferença entre Let e Where é que: o let vincula expressões a si e o where vincula somente construtores sintáticos. 
"Em haskell, toda expressão e função tem que retornar alguma coisa."
"O cálculo do comprimento de uma lista genérica, pode ser definido por uma função ""len"", a partir das cláusulas ""len [ ] = 0"" e ""len (h:t) = 1 + len(h)"". "
"A função dólar, definida como f ($) x = f x é utilizada como alternativa aos parênteses na definição da precedência de aplicações de funções."
"Em geral, o operador $ é usado para composição de funções, enquanto o operador . é usado para aplicação de funções, permitindo combinar e aplicar funções de diferentes maneiras."
"A programação funcional incentiva o uso de expressões lambda, que são funções anônimas que podem ser passadas como argumentos ou retornadas por outras funções."
"""deriving(Read)"", após a definição de um tipo de dados, permite gerar estruturas daquele dado a partir de strings. Para isso, a representação do dado em string deve ser idêntica à representação exibida pela função ""show""."
"Se o método “show” em Haskell faz a amostragem de um tipo, ele pode fazer a amostragem de uma lista do tipo, um exemplo seriam os inteiros"
"A ""Lazy Computation"" consiste em apenas avaliar trechos do código que serão absolutamente necessários a fim de executar o algoritmo."
"Programação funcional, programação orientada a objetos e programação lógica são exemplos de paradigmas de programação declarativos."
"Em Haskell, é possível definir funções de alta ordem, ou seja, que recebem outras funções como argumentos ou retornam funções como resultado. "
Java só é lazy quando se manuseiam streams
"Dada a função ""s x y = x + y"" com x e y inteiros, sua definicão de tipo é s :: Int -> (Int -> Int)"
"A função f s = s == reverse s retorna verdadeiro para a String ""arara"" e falso para a string ""papagaio"""
Uma função da typeclass “Show” pode ser printada
"Em um programa feito na linguagem Haskell, nem todas as cláusulas precisam ser comparáveis"
A filtragem de objetos com parâmetros específicos em Java é feita através do método filter()
"As funções locais definidas com ""where"" não são acessíveis fora da função principal onde foram declaradas."
"A programação funcional não permite o efeito colateral, o que significa que uma função pura sempre produzirá o mesmo resultado para a mesma entrada."
"Na programação funcional, é comum usar recursão em vez de laços de repetição para realizar iterações."
A programação funcional é um paradigma de programação que enfatiza o uso de funções puras e evita o uso de estados mutáveis.
"Em Haskell, F x = G 5 x é o mesmo que F = G 5."
"De forma simplificada, Lazy Evaluation consiste em esperar para processar uma expressão (ou parte dela) até o exato momento em que o resultado é utilizado."
- [ ] Eq em Haskell é uma class type ou tipo de tipo
len [] = 0 len(h:t) = 1+ len t O codigo acima calcula o tamanho de uma lista?
"A definição da cláusula ""fat n = n * fat n - 1"" em haskell é suficiente para calcular o fatorial de qualquer número n."
Funções de alta ordem são funções que podem receber outras funções como parâmetro
Laziness é um conceito intrínseco à toda linguagem funcional
"A stream em Java segue a “lazy evaluation”, ou seja, passa os objetos um a um apenas quando “solicitado”"
o Maybe é uma abordagem que evita o uso de if-else em programação funcional
A linguagem de haskell é fortemente tipada?
"Em Haskell, é possível usar funções dentro de comparação para retornar um valor booleano."
"As linguagens funcionais possibilitam escrever códigos curtos, rápidos e de fácil manutenção."
Linguagens funcionais não permitem a implementação de métodos de ordenação
A implementação mais próxima do paradigma funcional em C está relacionada ao uso de ponteiro para funções.
Haskell tem uma sintaxe bidimensional
"Como em Haskell não é possível alterar o valor de variáveis, não existem structs"
"O código Haskell:  x=[1,5..100] ,gera uma lista que vai de 1 até 100, com um intervalo de 4 em 4?"
"Um dos problemas em Haskell é que a linguagem não permite a definição de tipos de dados, os quais podem ter múltiplos construtores e parâmetros."
A recursão é uma das técnicas em programação funcional para suprir a falta de laços iterativos.
"Dada a função ""f x y = x + y"", o seu tipo é ""f :: Num a => a -> a -> a"", podendo ser utilizada tanto com números inteiros quanto em reais."
"A função: p = f [2..] where f (p:xs) = p : f [x | x <- xs, x `mod` p /= 0] retorna uma lista que pode ser impressa sem correr o risco de entrar em loop infinito, devido à Haskell ser lazy"
"Na chamada ""lazy evaluation"", todas as expressões são calculadas imediatamente, mesmo que seus resultados não sejam utilizados"
"Em Haskell, tanto o ""$"" quanto o ""."" são funções que recebem uma função como argumento e retornam uma função"
"A expressão ""where"" em haskellé usada para definir funções locais dentro de outras funções, permitindo a criação de funções auxiliares dentro de uma função principal"
"Em C, na expressão ""int (*filtro) (struct Aluno)"", o parâmetro ""filtro"" é um ponteiro para uma função que recebe uma struct Aluno como parâmetro e retorna um inteiro.  "
"A ""main"" em Haskell é do tipo IO, ou seja, pode ""mudar o mundo"""
"O ""case"" em haskell é amplamente utilizado para realizar expressões de casamento de padrões em listas e tuplas"
"A função  f s = p == reverse p  where p = map toLower $ filter isAlphaNum s , quando aplicada à string ""A man, a plan, a canal, Panama!"", retorna:"
Ruby é uma linguagem de programação preguiçosa.
Linguagens de programação que usam o paradigma funcional devem implementar o Lazy Evaluation.
"Haskell, java com strings e JavaScript são todas linguagens ""preguiçosas""."
Maybe é uma maneira de lidar com o valor nulo ou vazio em programação funcional
Haskell é a única linguagem que utiliza funções puras.
O principal aspecto da programação funcional é ser baseada em funções puras.
Haskell suporta herança de classes?
Linguagens funcionais são especialmente adequadas para lidar com a concorrência de recursos em sistemas distribuídos.
"na linguagem haskell, o tipo Int é geralmente mais eficiente em termos de uso de memória e desempenho em comparação com Integer"
Closure é um conceito usado apenas em linguagens de programação funcional
Haskell é uma linguagem em que tudo é função.
"A programação funcional é um paradigma de programação que se baseia principalmente em estruturas de repetição, como laços for e while."
"Diferentemente da programação funcional em haskell, em java existe uma função ""peek"" que pode ser usada para depuração ou para observar o fluxo de elementos sem modificá-los."
"Suponha uma função na linguagem C, ""function(int var);"" . Em seguida, na main foi escrito a seguinte linha, ""int x = 5; if(function(x) == function(x)){...}. Nesse caso, o código sempre executará o código dentro desse if."
"É possível implementar conceitos funcionais em versões de linguagens imperativas modernas, como Javascript, Java e C++"
"A programação funcional é exclusiva de linguagens de programação funcionais, como Haskell e Lisp, e não pode ser usada em linguagens imperativas. "
Select de ruby tem função similar a filter de Haskell.
"Em Haskell, se uma função imprime algo no terminal, essa função deve retornar um tipo IO"
function' :: (Num b) => [a] -> b \n  function' [] = 0  \n function' (_:xs) = 1 + function' xs \n Essa função calcula o comprimento de uma lista.
A função lambda sempre requer a especificação do tipo de seus argumentos
"Java e C++ possuem possibilidade funcional, apesar de C++ ser uma opção melhor para o uso funcional"
A ordem de consideração dos parâmetros de uma função em haskell é sempre da esquerda para a direita?
É possível chamar outra função dentro de uma função diferente? 
const f = (x) =>{ 	return x + 42; }  const f = (x) => (x + 42);  const f = x => x + 42; As declarações acima são equivalentes?
A programação funcional se baseia no uso de funções como parte fundamental na construção de programas
A programação funcional em Java também é lazyness.
Haskell é uma linguagem de programação compilada?
"A função lambda é definida diretamente na expressão em que será usada, não precisando de palavras-chave como ""def"" ou ""function"""
"Programação funcional consiste em passar trechos de código que se repetem como parâmetro de uma função,fazendo com que o código se torne mais flexível e otimizado. "
Soluções recursivas são padrão em linguagens funcionais
"A avaliação preguiçosa (lazy) em Haskell permite que todas as expressões sejam avaliadas imediatamente, mesmo que não sejam necessárias para o resultado final do programa?"

